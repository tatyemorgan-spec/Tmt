---
description: Score a call transcript and update the libraries
argument-hint: [transcript file]
---
Run the call-coach agent on: $ARGUMENTS (look in transcripts/ if only a name is given; if blank, use the newest transcript without a matching review).
Print the scores, 3 wins, 3 fixes and the rewritten line.
